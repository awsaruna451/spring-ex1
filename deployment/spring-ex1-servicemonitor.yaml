apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: spring-ex1-monitor
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: spring-ex
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s