apiVersion: v1
kind: Service
metadata:
  name: spring-ex-service
spec:
  selector:
    app: spring-ex
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
  type: NodePort